#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

class Solution {
public:
    int coinChange(vector<int>& coins, int amount) {
        
    }
};

int main()
{
    Solution sol;
    vector<int> coins;
    // coins.reserve(5, 1,2,3,4,5);
    int amount = 5;
    sol.coinChange(coins, amount);
}